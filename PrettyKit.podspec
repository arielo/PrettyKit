Pod::Spec.new do |s|
  s.name     = 'PrettyKit'
  s.version  = '0.2.0'
  s.homepage = 'https://github.com/sobri909/MGBox2'
  s.source   = { :git => 'https://github.com/creativemess/PrettyKit.git' }
  s.source_files = 'PrettyKit'
end
